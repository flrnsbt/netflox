import 'package:flutter/material.dart';

class UnknownPageScreen extends StatelessWidget {
  const UnknownPageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
